version: '1'
services:
  bot:
    container_name: 'AntiMahan'
    build:
      context: .
    stop_signal: SIGINT
    restart: unless-stopped
    command: "python3 main.py"
    volumes:
      - .:/app
      
      